# 環境変数の解決

手順書に出てくる `$AI_MONITOR_*` などの `$` 付きの名前は環境変数で、実値は持っていない。
使う前に `echo` で実値を確認してから操作に使う。

## 解決コマンド

```bash
echo "$AI_MONITOR_LABEL_IN_DISCUSSION"
```

- 複数の変数は `echo` に並べて 1 回で確認できる
- 値は `constants.env` 由来で、SessionStart フックがセッション開始時に展開済み
- 空で返る場合は名前の綴りを手順書と突き合わせる（推測した値で操作しない）
